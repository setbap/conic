import 'package:flutter/material.dart';

class ExchangesSearch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Tab(icon: Icon(Icons.directions_transit)),
    );
  }
}
